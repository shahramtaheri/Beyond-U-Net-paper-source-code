# configs/lidc_2d_fold5.yaml
defaults: configs/default.yaml

data:
  img_key: image
  mask_key: mask
  splits:
    fold_json: data/splits/fold_5.json

train:
  outdir: outputs/fold5

seed:
  seed: 42
  deterministic: true
