# configs/lidc_2d_fold1.yaml
defaults: configs/default.yaml

data:
  img_key: image
  mask_key: mask
  splits:
    fold_json: data/splits/fold_1.json

train:
  outdir: outputs/fold1

seed:
  seed: 42
  deterministic: true
