# configs/lidc_2d_fold2.yaml
defaults: configs/default.yaml

data:
  img_key: image
  mask_key: mask
  splits:
    fold_json: data/splits/fold_2.json

train:
  outdir: outputs/fold2

seed:
  seed: 42
  deterministic: true
