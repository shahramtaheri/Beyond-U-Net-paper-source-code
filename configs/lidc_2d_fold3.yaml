# configs/lidc_2d_fold3.yaml
defaults: configs/default.yaml

data:
  img_key: image
  mask_key: mask
  splits:
    fold_json: data/splits/fold_3.json

train:
  outdir: outputs/fold3

seed:
  seed: 42
  deterministic: true
